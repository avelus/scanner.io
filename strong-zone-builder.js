class StrongZoneBuilder{build(z1,z2){const out=[];for(const a of z1){for(const b of z2){if(a.rangeLow<=b.rangeHigh&&b.rangeLow<=a.rangeHigh){out.push({mergedRangeLow:Math.min(a.rangeLow,b.rangeLow),mergedRangeHigh:Math.max(a.rangeHigh,b.rangeHigh),combinedDensity:a.totalDensity+b.totalDensity,dominantPeakPrice:a.dominantPeakLiquidity>b.dominantPeakLiquidity?a.dominantPeakPrice:b.dominantPeakPrice})}}}return out}}